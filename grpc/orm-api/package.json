{
  "name": "orm-api",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "server": "node src/server.js",
    "client": "node src/client.js",
    "prisma:generate": "prisma generate",
    "prisma:db:push": "prisma db push",
    "seed": "node prisma/seed.js",
    "db:setup": "npm run prisma:generate && npm run prisma:db:push && npm run seed"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.13.4",
    "@grpc/proto-loader": "^0.8.0",
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
